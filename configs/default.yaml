learning_rate: 0.0001
batch_size: 8
num_epochs: 50
image_height: 512
image_width: 512

# ── Week 2: Segmentation Training ──────────────────────────────────────────
data:
  root_dir: data/camvid/
  image_height: 360
  image_width: 480
  num_classes: 32
  num_workers: 2

training:
  epochs: 20
  batch_size: 4
  learning_rate: 0.001
  checkpoint_dir: checkpoints/
